# Local M4 micro-pilot size. Stamps power_status=micro; powered n>=512 is a
# future scale target (see README / DESIGN), not the current claim.
name: pilot
n_items: 32
seed: 0
train_frac: 0.6
val_frac: 0.2
test_frac: 0.2
shuffle: true
max_prompt_tokens: 512
